# AGENTS.md

## 项目定位

本仓库是面向 500 人以上企业的私有化项目管理平台。核心目标是提高项目进度透明度并改善跨部门协作，覆盖内部项目、临时任务以及客户参与的需求交付场景。

所有实现都应围绕以下主链路：

`提出需求 → 待分派需求池 → 分配负责人 → 完善及审批需求 → 关联已有项目或创建新项目 → 任务执行 → 提交交付版本 → 客户验收 → 完成或退回修改`

## 事实来源与修改原则

- `docs/01-PRD.md` 是产品范围与业务规则的主基线。
- `docs/02-MVP与版本规划.md` 决定功能属于当前版本还是后续版本。
- `docs/03-角色权限矩阵.md` 是角色能力与数据范围的基线。
- `docs/04-业务流程.md` 是需求、交付、催办、合并和状态迁移的基线。
- `docs/05-功能模块与页面结构.md` 是页面与信息架构基线。
- `docs/06-核心数据模型.md` 是领域实体与关键约束基线。
- `docs/07-待确认事项.md` 中未决事项不得擅自固化为不可逆业务规则。
- 当代码、README 与需求文档冲突时，先指出冲突；若用户没有明确改变需求，以需求文档为准，并同步更新受影响文档。
- 新增业务能力时，应同时检查权限、数据隔离、审计、通知、国际化和迁移影响，避免只完成界面或单一接口。

## 技术基线

- 后端：Java 21、Spring Boot 4.1、Spring MVC、Spring Data JPA、Spring Security。
- 前端：Vue 3、TypeScript、Vite、Pinia、Vue Router、Axios。
- 数据库：MySQL 8.4；Flyway 管理正式数据库结构。
- 测试：后端使用 Spring Boot Test 与 H2；前端至少执行 TypeScript 和生产构建检查。
- 部署：当前支持 Docker Compose；目标是企业私有化部署。
- 默认语言为中文，同时为中英文国际化保留边界。

仓库结构：

- `backend/`：Spring Boot API。
- `frontend/`：Vue 3 Web 客户端。
- `docs/`：产品和业务设计基线。
- `docker-compose.yml`：本地容器化运行入口。

## 核心领域约束

- 项目层级采用 `项目 → 里程碑 → 任务 → 子任务`；需求独立存在，可关联已有项目，也可在审批后创建新项目。
- 任务至少包含负责人、参与人、开始时间、截止时间、实际时间、优先级、状态和预计工时。
- 任务完成时必须填报实际工时；不得绕过完成/交付语义直接制造已验收状态。
- 客户验收结果包括通过、驳回、要求修改。驳回或要求修改应退回原任务，并保留每次交付版本和验收意见。
- 验收超期只催办，不自动通过。
- 风险必须记录风险等级；文档必须保留历史版本。
- 预算、人工成本、其他费用和预算使用率采用手工维护；客户成本可见性仍以待确认事项和授权规则为准。
- 审批流程和状态机由项目类型决定。状态迁移既要校验流程定义，也要校验操作者权限。

## 权限与数据隔离

- 不得仅依靠前端隐藏按钮实现权限控制；所有读取和写入都必须在后端校验身份、角色、对象关系和数据范围。
- 客户只能访问与自己关联的需求、项目进度、交付和授权文档，不能因为拥有 `CUSTOMER` 角色而访问同类全部数据。
- 部门负责人仅查看本部门成员及任务，不默认获得相关项目的全部数据。
- 项目经理可管理本项目作业并跨部门指定成员，但不能代替客户进行正常验收。
- 项目成员只能访问被授权或与自己相关的项目、任务和文档。
- 管理员异常修改状态或验收结果时必须记录原因和审计信息。
- 新接口默认最小权限；列表、统计、导出和搜索接口同样必须应用数据范围过滤。

## 合并规则

- 项目、任务和需求的合并仅允许管理员执行。
- 合并前必须展示目标对象、迁移范围和冲突；不得静默覆盖字段。
- 评论、附件、文档版本、工时、审批、风险、交付、通知关联等业务记录应迁移或重建关联。
- 来源对象默认保留为只读的“已合并”记录，保留原编号、目标映射和审计日志，不直接物理删除。
- 当前 MVP 尚未提供完整合并执行器。实现前必须先解决 `docs/07-待确认事项.md` 中的冲突策略与撤销边界。

## 工作日历与资源负荷

- 当前 MVP 尚未实现完整资源日历，不要用简单的自然日差值冒充最终可用工时。
- 资源负荷设计必须区分个人可用工时、已分配预计工时、实际工时、请假/节假日和排期冲突。
- 资源与负荷查询必须遵守数据范围：项目经理看本项目，部门负责人看本部门，成员看本人。
- 在工作日历规则最终确认前，相关计算应保持可配置，并在接口或界面中明确计算口径。

## 后端实现规则

- 包名保持在 `com.rcai.pm` 下，并优先按业务领域组织，而不是按 Controller/Service/Repository 技术层全局拆分。
- Controller 负责协议、校验和响应；业务规则放在 Service；Repository 仅负责持久化查询。
- 使用 Jakarta Validation 校验请求；业务错误通过统一异常处理返回，不在 Controller 中散落响应拼装。
- 涉及多表写入、状态迁移、交付和合并的操作应使用事务。
- 实体枚举和值必须与数据库定义和前端类型同步。
- 不向日志、源码或提交中写入密码、令牌、连接密钥或客户敏感数据。

## 数据库规则

- 已应用的 Flyway 迁移不得修改；结构变更必须新增按顺序编号的迁移文件。
- MySQL 是正式运行基线。H2 测试通过不能替代 MySQL 迁移和 Hibernate schema validation 验证。
- 新表应明确主键、外键、必要索引、唯一约束、时间字段和审计需求。
- 删除或合并业务对象优先使用状态与映射保留追溯性，不默认物理删除。

## 前端实现规则

- 使用 Vue 3 Composition API 与 TypeScript，公共请求统一通过 `frontend/src/api/http.ts`。
- 登录态和跨页面共享状态放入 Pinia；页面路由访问必须配合后端授权，不把前端路由守卫视为安全边界。
- 新增状态、角色或 DTO 时同步更新 `frontend/src/api/types.ts`。
- 所有核心操作需要清晰的加载、空状态、成功和错误反馈；危险或不可逆操作需要二次确认。
- 页面保持响应式，兼顾桌面和移动端；不要在未确认前引入独立原生客户端实现。

## 验证要求

修改后按影响范围执行：

```bash
cd backend && mvn test
cd frontend && npm run build
```

涉及数据库迁移或 JPA 映射时，还必须使用 MySQL 8.4 启动后端，确认 Flyway、Hibernate schema validation 和 `/actuator/health` 正常。

涉及登录、CSRF、权限或数据隔离时，至少验证：未登录、无权限角色、允许角色、越权对象访问四类场景。

涉及关键页面布局或交互时，应在实际浏览器中验证登录、主链路、控制台错误和常见桌面/移动视口。

## 当前版本边界

当前 `0.1.0 MVP` 已覆盖账号登录、多角色账号、项目/里程碑/任务、基础看板、需求池分派与审批、基础数据范围、工时完成、交付版本以及客户通过/驳回/要求修改。

以下仍属于后续迭代或未完成能力：高级流程设计器、完整合并执行器、工作日历与资源负荷、邮件/企业微信/Teams、统一身份认证、文件存储与文档管理、甘特图、预算成本完整界面和综合报表。不要在交付说明中把预留的数据结构描述成已经完成的功能。
